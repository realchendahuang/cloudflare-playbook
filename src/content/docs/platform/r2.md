---
title: R2
description: Cloudflare R2 的定位、适用场景和成本边界。
---

R2 是 Cloudflare 的对象存储，适合放文件、图片、附件、备份、导入导出结果和数据湖对象。数据库看 D1，前端构建产物看 Static Assets / Pages。

## 先判断

| 场景 | 建议 |
| --- | --- |
| 用户上传图片、附件、导出文件 | 优先用 R2，数据库只存对象标识和状态。 |
| 公开下载、软件包、PDF、备份 | 适合，但要看读取次数。 |
| 原图存储 + 缩略图处理 | 原图进 R2，转换看 Images 或 Worker。 |
| D1 里想存大段内容、图片、二进制 | 不适合，D1 存索引，R2 存对象。 |
| 前端构建产物 | 不优先，Static Assets / Pages 更合适。 |
| 视频产品 | 不优先，完整播放体验看 Stream。 |
| 高频小对象读写 | 谨慎，先设计缓存和索引。 |

## 成本先看什么

R2 成本看三件事：存了多少、写了多少、读了多少。它的 egress 免费很有价值，但不代表对象存储本身完全免费。

| 成本信号 | 判断 |
| --- | --- |
| 存储容量 | 小项目文件、图片、备份可以起步；长期堆文件要做生命周期和清理。 |
| 写入类操作 | 上传、写入、列举、生命周期等偏修改操作会增长。 |
| 读取类操作 | 读取、下载、查看对象信息等偏读取操作会增长，公开下载热点尤其要看。 |
| 出站流量 | R2 的优势是出站带宽不收费，但读写操作和存储仍要算。 |
| 低频访问存储 | 冷数据再用，热文件优先 Standard。 |

## 设计原则

| 原则 | 判断 |
| --- | --- |
| 数据库只存引用 | D1 存对象标识、归属、大小、状态和权限。 |
| 上传要有权限边界 | 用户上传先经过鉴权和签名。 |
| 下载区分公开和私有 | 公开文件可走自定义域名；私有文件用签名 URL 或 Worker 鉴权。 |
| 大文件处理 | Worker 做鉴权和路由，尽量流式处理。 |
| 热点读要加缓存 | 公开下载和图片访问要考虑 CDN 缓存。 |
| 冷数据再考虑 IA | 低频访问存储适合冷数据，不适合热文件。 |
