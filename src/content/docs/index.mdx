---
title: Cloudflare Playbook
description: 把 Cloudflare 免费额度讲透，再选择产品、架构和 5 美元/月 Workers Paid 的实践知识库。
template: splash
hero:
  tagline: 面向独立开发者、小团队和开源项目。先把免费额度看透：0 元能跑什么、哪里会撞墙、5 美元/月 Workers Paid 买到什么。
  actions:
    - text: 免费额度大全
      link: /platform/free-paid/
      icon: open-book
    - text: 学习路线
      link: /start/
      icon: right-arrow
      variant: minimal
---

import { Aside, Card, CardGrid } from '@astrojs/starlight/components';

## 免费额度先讲透

Cloudflare Playbook 的第一入口就是免费额度。普通项目最关心 0 元能跑到哪里、哪一层会先撞墙、5 美元/月 Workers Paid 到底买到什么。价格、额度和计划边界会变化，这里列出的数字以官方价格、限制和账单页面为最终依据；首页先放最影响架构的数字，完整口径见 [免费额度大全](/platform/free-paid/)。

<Aside type="tip" title="先看这条主线">
能静态化的先放静态资产层；需要关系数据的用 D1；读多写少的配置用 KV；文件进 R2；公开写入口加 Turnstile；只有动态接口、回调、评论、搜索代理和后台任务才进 Worker。
</Aside>

| 先问 | 直接答案 |
| --- | --- |
| 文档站、官网、博客能不能 0 元跑？ | 可以。静态资产请求免费且不限量，先用 Workers Static Assets / Pages。 |
| 小接口、评论、表单能不能 0 元验证？ | 可以。Workers Free、D1、KV、R2、Queues、Durable Objects 都有免费层。 |
| 搜索和轻社区能不能先不买服务？ | 可以。Pagefind 解决静态搜索；Twikoo Cloudflare + D1 可做轻评论；AI 搜索等确认价值后再上。 |
| 免费阶段最该防什么？ | 防公开写入口被刷、防静态流量误进 Worker、防数据库全表扫描、防 AI 调用不设限。 |
| 什么时候付 5 美元/月？ | 动态请求、CPU、日志、队列、D1/KV/DO 等开发者平台能力进入真实生产后再付。 |

## 先记住这 6 个免费数字

这 6 个数字决定大多数普通项目的起步架构。先按它们放产品，完整口径再看下方表格。

| 免费数字 | 代表什么 | 对普通项目的意义 |
| --- | --- | --- |
| 静态资产请求免费且不限量 | Workers Static Assets / Pages 命中静态资源时不计动态 Worker 请求。 | 文档站、官网、博客和前端资源先静态化，主流量先不花钱。 |
| 100,000 次 Worker 请求/天 | Workers Free 的动态请求日额度。 | 小接口、评论、表单、回调、搜索代理可以先验证真实需求。 |
| 10 ms CPU/次调用 | Workers Free 的单次调用 CPU 边界。 | 鉴权、轻逻辑和代理适合；SSR、AI 前处理、大文件解析要谨慎。 |
| D1 5M 读 / 100k 写 / 天 | D1 Free 的每日行读取和写入额度。 | 评论、反馈、小后台可以起步；关键是建索引和限制公开写入。 |
| R2 10 GB / 1M 写 / 10M 读 / 月 | R2 Standard 的免费存储和操作额度。 | 附件、图片原图、导出文件先放 R2，公开下载配缓存。 |
| Turnstile 验证不限量 | Free 账号 20 个 widget，每个 widget 10 个域名。 | 评论、表单、上传、登录、AI 搜索入口先有防刷边界。 |

## 免费额度速查大表

先把这张表当成架构清单：能放免费层就先放免费层；撞到右侧信号，再去核对 Workers Paid、域名计划或单独计费产品。

| 层 | 免费额度 / 免费边界 | 最适合先做 | 撞墙信号 |
| --- | --- | --- | --- |
| 静态资产 | Workers Static Assets / Pages 命中静态资源时，请求免费且不限量。 | 文档站、官网、博客、前端构建产物。 | 静态资源被 Worker 处理，开始消耗动态请求和 CPU。 |
| Pages 构建 | Pages Free 每月 500 次构建、1 个并发构建、每站 20,000 个文件。 | Git 驱动的静态站和预览环境。 | 多分支预览频繁触发构建，或站点文件数明显变大。 |
| Workers Builds | Free 每月 3,000 分钟构建、1 个并发构建。 | 用 Workers 平台构建和部署前端项目。 | 构建时间长、多人同时发版、构建队列开始排队。 |
| 动态接口 | Workers Free 100,000 次请求/天，10 ms CPU/次调用。 | 小接口、评论、表单、第三方回调、搜索代理。 | 日请求接近上限，或单次处理经常超过 10 ms CPU。 |
| Worker 日志 | Workers Logs Free 每天 200,000 条日志事件，保留 3 天。 | 早期排障、观察错误、确认接口是否被刷。 | 需要 7 天以上留存、长期审计或外部日志平台。 |
| 关系数据 | D1 Free 5M 行读取/天、100k 行写入/天、5 GB 总存储。 | 评论、反馈、小后台、轻量业务表。 | 无索引查询、全表扫描、写入入口被刷。 |
| 配置缓存 | KV Free 每天 100k 读取、1k 写入、1k 删除、1k 列举，1 GB 存储。 | 配置、开关、低频缓存、公开索引。 | 写多、强一致、需要事务或频繁列表查询。 |
| 对象存储 | R2 Standard 每月 10 GB、1M 写入类操作、10M 读取类操作，出站带宽免费。 | 附件、图片原图、导出物、下载文件。 | 热点文件没有缓存，读取类操作快速上涨。 |
| 异步任务 | Queues Free 10,000 次操作/天，消息保留 24 小时。 | 邮件、通知、导入、后处理、失败重试。 | 消息大于 64 KB、重试多、读写删操作被低估。 |
| 强一致状态 | Durable Objects Free 100,000 次请求/天、13,000 GB-s/天。 | 房间、会话、限流器、实时状态。 | WebSocket 长连不休眠，运行时长开始主导成本。 |
| 数据库连接 | Hyperdrive Free 100,000 次数据库查询/天。 | 连接既有 Postgres/MySQL 的小流量接口。 | 外部数据库成为核心路径，查询量和延迟都上来。 |
| 公开写入口 | Turnstile Free 每账号 20 个小组件，每个小组件 10 个域名，挑战和验证请求不限量。 | 登录、评论、表单、上传、AI 搜索入口。 | 需要更多域名、更复杂风控或企业级管理。 |
| 团队访问 | Zero Trust Free 50 个用户；Tunnel 可发布公开主机名。 | 小团队后台、预览环境、内网工具。 | 团队人数、策略、审计和设备管理变复杂。 |
| DNS | 新 Free zone 每个域名 200 条记录；2024-09-01 前创建的 Free zone 默认 1,000 条。 | 接入域名、代理 Web 流量、保护源站。 | 多租户、大量子域名或企业级 DNS 管理。 |
| WAF 自定义规则 | Free 有 5 条自定义规则；限流规则 1 条。 | 后台、登录、评论、上传、回调路径保护。 | 需要更多规则、更长统计周期、按 Header/Cookie 等维度限流。 |
| 图片转换 | Images Free 每月 5,000 次独立图片转换。 | 少量封面图、多尺寸缩略图、格式优化。 | 大量商品图、用户上传图、多尺寸分发。 |
| 浏览器运行 | Browser Run Free 每天 10 分钟浏览器时间，Browser Sessions 3 个并发浏览器。 | 少量截图、PDF、动态页面抓取验证。 | 能用普通请求却用了浏览器，或抓取任务变成常态。 |
| 埋点治理 | Zaraz 每账号每月 1,000,000 个免费事件。 | 第三方脚本治理、基础事件、同意管理。 | 埋点量大、营销脚本复杂或团队需要更细治理。 |
| Workers AI | Free 和 Paid 都有每天 10,000 Neurons 免费分配。 | 小模型、短输出、摘要、标签、轻量问答。 | 没有缓存、限流和模型选择，公开入口被放大调用。 |
| AI Gateway | 核心功能免费；Workers Free 持久日志为所有网关合计 100,000 条。 | 统一代理外部模型、缓存、限流、观察成本。 | 长期日志、统一账单、外部模型调用量上来。 |
| AI Search | 2026-04-16 后新实例公开测试期内免费；Free 每月 20,000 次查询、每天 500 个爬取页面。 | 文档自然语言搜索原型。 | Pagefind 已不够，且查询量、抓取量、模型成本都能被控制。 |
| Vectorize | Free 每月 30M 查询向量维度、5M 存储向量维度。 | 小规模语义搜索和 RAG 原型。 | 文档量大、维度高、查询频繁，向量成本开始可见。 |
| 预算提醒 | 可按美元阈值发邮件提醒按量费用。 | 付费前的账单观察。 | 需要硬封顶时要靠架构限流，预算提醒本身不会停服务。 |

## All the fun：0 元起步配方

普通项目先把静态内容、动态入口、数据、文件、安全和搜索放到正确位置。0 元阶段也能组合出完整项目：

| 项目 | 0 元组合 | 先看哪条线 |
| --- | --- | --- |
| 开源文档站 | Workers Static Assets / Pages + Pagefind + Web Analytics | 静态请求免费且不限量；Pages 每月 500 次构建。 |
| 教程站轻社区 | 静态资产层 + Twikoo Cloudflare + D1 + Turnstile | D1 每天 100k 行写入；公开入口要防刷。 |
| 小接口 / 第三方回调 | Workers Free + D1 / KV + 最小 WAF / 限流 | 100,000 次动态请求/天；10 ms CPU/次调用。 |
| 表单 / 反馈箱 | Workers Free + D1 + Turnstile + Workers Logs | D1 写入、日志 3 天留存、垃圾提交。 |
| 上传下载工具 | R2 + 签名链接 + CDN 缓存 | 每月 10 GB 标准存储；1M 写入类和 10M 读取类操作。 |
| 小团队后台 | Access + Tunnel + D1 | Zero Trust Free 50 个用户；后台入口先有身份边界。 |
| AI 搜索原型 | Pagefind + AI Gateway + Workers AI / AI Search | 先用静态搜索；AI 调用必须有缓存、限流和日志。 |
| 实时小应用 | Durable Objects + 连接休眠 + Queues | 房间状态、后台任务和重试都能先小规模验证。 |

## 5 美元/月买到什么

Workers Paid 是账号级开发者平台订阅，最低 5 美元/月。它把动态能力从日额度验证推进到可生产运行的月度包含量。

| 能力 | Free 起步 | Workers Paid 起步 |
| --- | --- | --- |
| Worker 请求 | 100,000 次/天。 | 10M 次/月，超出后按量计费。 |
| CPU | 10 ms/次调用。 | 30M CPU ms/月，单次默认 30 秒、最高 5 分钟。 |
| Workers Logs | 200k 条/天，保留 3 天。 | 20M 条/月，保留 7 天。 |
| KV | 100k 读/天、1k 写/天、1 GB 存储。 | 10M 读/月、1M 写/月、1 GB 存储，超出按量。 |
| D1 | 5M 行读/天、100k 行写/天、5 GB 存储。 | 25B 行读/月、50M 行写/月、5 GB 存储，超出按量。 |
| Queues | 10k 操作/天，消息保留 24 小时。 | 1M 操作/月，默认保留 4 天，可配置到 14 天。 |
| Durable Objects | 100k 请求/天、13k GB-s/天。 | 1M 请求/月、400k GB-s/月，超出按量。 |
| Browser Run | 10 分钟/天、3 个并发浏览器。 | 10 小时/月、10 个并发浏览器。 |

Workers Paid 不会自动提高域名计划权益。WAF、Bot、缓存规则、证书、负载均衡、R2、Images、Stream、企业网络和硬预算封顶仍然要按各自产品判断。

## 免费额度决定架构

先把项目拆成几层，再把每一层放到最合适的免费额度里。这样写出来的架构更稳，也更容易知道哪一天该升级。

| 架构层 | 最先看的免费额度 | 起步做法 |
| --- | --- | --- |
| 静态内容 | 静态资产请求免费且不限量；Pages Free 每月 500 次构建。 | 文档、官网、博客、前端资源先走 Workers Static Assets / Pages。 |
| 动态接口 | Workers Free 100,000 次请求/天，10 ms CPU/次调用。 | 只让接口、第三方回调、评论、表单和后台入口进 Worker。 |
| 关系数据 | D1 Free 5M 行读取/天、100k 行写入/天、5 GB 总存储。 | 评论、反馈、小后台先用 D1；常查字段先建索引。 |
| 配置缓存 | KV Free 每天 100k 读取、1k 写入、1 GB 存储。 | 放配置、开关、低频缓存；写多和强一致状态改用 D1 或 Durable Objects。 |
| 文件对象 | R2 Standard 每月 10 GB、1M 写入类操作、10M 读取类操作，出站带宽免费。 | 附件、图片原图、导出物放 R2；公开下载配缓存。 |
| 异步任务 | Queues Free 10,000 次操作/天。 | 邮件、通知、导入、后处理进队列；重试和消息大小要计入操作数。 |
| 强一致状态 | Durable Objects Free 100,000 次请求/天、13,000 GB-s/天。 | 房间、会话、限流器用 DO；实时连接优先做休眠。 |
| 公开写入口 | Turnstile Free 20 个小组件，每个小组件 10 个域名。 | 登录、评论、表单、上传和 AI 搜索入口先加防刷边界。 |
| AI 与搜索 | Pagefind 不打后端；Workers AI 每天 10,000 Neurons；AI Search 新实例每月 20,000 次查询。 | 静态搜索先 Pagefind；自然语言搜索验证后再接 AI Gateway / Workers AI / AI Search。 |

## 免费阶段最容易撞的红线

免费额度最怕入口放错。先把这些红线写进项目设计里，后面就少很多账单和排障焦虑。

| 红线 | 为什么危险 | 起步做法 |
| --- | --- | --- |
| 静态资源误进 Worker | 图片、脚本、样式和 HTML 会消耗动态请求与 CPU。 | 静态内容先放 Workers Static Assets / Pages，只让接口进 Worker。 |
| 公开写入口直接暴露 | 评论、表单、上传、搜索和第三方回调会被刷。 | Turnstile、限流、鉴权和业务配额先上。 |
| D1 无索引查询 | D1 按读取行数计量，全表扫描会浪费免费额度。 | 常查字段建索引，列表查询分页，后台报表离线跑。 |
| KV 写多或强一致 | KV 免费写入少，且一致性边界不同。 | 配置和低频缓存用 KV；状态、评论、订单用 D1 或 Durable Objects。 |
| R2 热点读取无缓存 | R2 免出站带宽费，但读取类操作仍然计量。 | 公开下载配 CDN 缓存，私有下载用签名链接。 |
| AI 无缓存无上限 | Workers AI、AI Gateway、AI Search 和 Vectorize 都会被开放式调用放大。 | Pagefind 先顶上；AI 入口加缓存、限流、模型选择和日志。 |

<Aside type="caution" title="先记住这条">
免费额度先服务架构顺序：静态内容 -> 静态层；文件 -> R2；关系数据 -> D1；配置缓存 -> KV；强一致状态 -> Durable Objects；耗时任务 -> Queues；公开写入口 -> Turnstile。
</Aside>

<CardGrid>
	<Card title="静态站主流量免费" icon="cloud-download">
		Workers Static Assets / Pages 命中静态资源时，请求免费且不限量。文档站、官网、博客和前端资源要先停在这里。
	</Card>
	<Card title="动态能力先用 Free 验证" icon="server">
		Workers Free 有 100,000 次动态请求/天和 10 ms CPU/次调用。小接口、第三方回调、表单和评论可以先从这里起步。
	</Card>
	<Card title="数据和文件都有免费层" icon="database">
		D1、KV、R2、Queues、Durable Objects 都有可用免费额度。重点是按数据形态放对位置。
	</Card>
	<Card title="安全基础先上" icon="padlock">
		Universal SSL、DDoS、基础 WAF、Turnstile、Access 和 Tunnel 都能在免费阶段解决不少真实风险。
	</Card>
	<Card title="AI 先小步试验" icon="rocket">
		Pagefind 先解决静态搜索；自然语言搜索明确有价值后，再接 AI Gateway、Workers AI、AI Search 或 Vectorize。
	</Card>
	<Card title="5 美元/月看清边界" icon="warning">
		Workers Paid 放大开发者平台能力，不自动提升 WAF、Bot、证书、缓存规则、负载均衡或企业网络权益。
	</Card>
</CardGrid>

## 什么时候付 5 美元/月

它最适合在 Free 已经证明项目有人用之后再开。

| 该付的信号 | 为什么 |
| --- | --- |
| Worker 请求稳定接近 100,000 次/天。 | Free 会在日额度耗尽后出问题；Paid 改成月度已包含用量。 |
| CPU 经常超过 10 ms。 | 服务端渲染、AI 前处理、批处理和大内容解析需要更高 CPU 上限。 |
| 日志 3 天不够排障。 | Paid Workers Logs 留存和月度额度更适合生产。 |
| D1、KV、Queues、Durable Objects 已经是核心功能。 | 这些产品在 Paid 下进入更高月度额度和付费扩展。 |
| 需要更多定时任务、子请求、Worker 数量或更大构建包。 | 这是开发者平台工程化能力。 |

| 不该误会 | 正确理解 |
| --- | --- |
| 5 美元/月和 Cloudflare Pro 分开看。 | 它不增加 WAF、缓存规则、Bot、证书或域名计划权益。 |
| Static Assets 只对静态资产免费。 | 命中静态资产才免费；服务端渲染、动态函数、接口、整站先跑 Worker 会走 Workers 计费。 |
| R2 出站带宽免费仍有存储和操作成本。 | 存储量、写入类操作和读取类操作仍然要看。 |
| 预算提醒只发邮件。 | 它不会暂停服务或阻止继续计费。 |
| AI 免费额度需要入口控制。 | 必须做缓存、限流、模型选择和日志观察。 |

## 继续读

- [免费额度大全](/platform/free-paid/)
- [Cloudflare 产品索引](/platform/)
- [账单与预算](/platform/billing/)
- [成本控制](/best-practices/cost/)
- [独立开发者推荐栈](/best-practices/indie-stack/)
- [文档站技术栈](/best-practices/site-stack/)
